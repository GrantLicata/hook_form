import UserForm from "./components/UserForm"
import './App.css';

function App() {
  return (
    <div className="App">
      <UserForm firstName={} lastName={} password={} confirmPassword={} />
    </div>
  );
}

export default App;
